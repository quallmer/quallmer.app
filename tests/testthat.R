# This file is part of the standard testthat directory layout.
# It is automatically run during R CMD check and by devtools::test().

library(testthat)
library(quallmer.app)

test_check("quallmer.app")
